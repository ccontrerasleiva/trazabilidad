@model ViewModels.Models.Conductor
@using Trazabilidad.Controllers;
@using Resources.Strings;

@using (Html.BeginForm(nameof(Trazabilidad.Controllers.ConductorController.Edit), nameof(Resources.Strings.Models.Conductor), FormMethod.Post))

{
    <div class="col-sm-12">
        <h3 class="m-t-none m-b">Editar Conductor</h3>

        <form role="form">
            @Html.HiddenFor(cl => cl.Id)
            <div class="form-group">
                @Html.LabelFor(cl => cl.Id, "Código")
                @Html.TextBoxFor(cl => cl.Id, htmlAttributes: new { @class = "form-control", placeholder = "Código del Conductor" })
            </div>
            <div class="form-group">
                @Html.LabelFor(cl => cl.Rut, "Rut")
                @Html.TextBoxFor(cl => cl.Rut, htmlAttributes: new { @class = "form-control", placeholder = "Rut" })
            </div>
            <div class="form-group">
                @Html.LabelFor(cl => cl.Nombres, "Nombre")
                @Html.TextBoxFor(cl => cl.Nombres, htmlAttributes: new { @class = "form-control", placeholder = "Nombre" })
            </div>
            <div class="form-group">
                @Html.LabelFor(cl => cl.Apellidos, "Apellido")
                @Html.TextBoxFor(cl => cl.Apellidos, htmlAttributes: new { @class = "form-control", placeholder = "Apellido" })
            </div>
            <div class="form-group">
                @Html.LabelFor(cl => cl.Celular, "Celular")
                @Html.TextBoxFor(cl => cl.Celular, htmlAttributes: new { @class = "form-control", placeholder = "Celular" })
            </div>
            <div class="form-group">
                @Html.LabelFor(tt => tt.Id, "Estado")

            </div>
            <div class="form-group">
                <div class="form-group">
                    @Html.RadioButtonFor(m => m.Deshabilitado, false, htmlAttributes: new { id = "Habilitado" }) @Html.Label("Habilitado", "Habilitado")
                    @Html.RadioButtonFor(m => m.Deshabilitado, true, htmlAttributes: new { id = "Deshabilitado" }) @Html.Label("Deshabilitado", "Deshabilitado")
                    @Html.ValidationMessageFor(x => x.Deshabilitado, null, new { @class = "text-danger" })
                </div>
            </div>
            <div>
                <button class="btn btn-sm btn-primary pull-right m-t-n-xs" type="submit"><strong><i class="fa fa-plus"></i> Guardar</strong></button>
            </div>
        </form>
    </div>

}
