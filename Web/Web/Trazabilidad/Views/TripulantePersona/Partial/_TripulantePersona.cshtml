@model ViewModels.Models.TripulantePersona
@using ViewModels.Enum;

<tr>
    @using (Html.BeginForm(nameof(Trazabilidad.Controllers.TripulantePersonaController.Edit),nameof(Resources.Strings.Models.TripulantePersona),FormMethod.Post)) {
        @*@Html.HiddenFor(x => x.Id)*@
        <td>@Model.Nombre</td>
        <td>@Model.Sindicato</td>
        <td>@Resources.Util.Enums.GetDescription((ViewModels.Enum.Cargo)Model.Cargo)</td>
        <td>@Model.Correo</td>
        <td>@Model.Telefono</td>

        if (@Model.Deshabilitado == false) {
            <td>Habilitado</td>} else {
            <td>Deshabilitado</td>}

            <td>@Model.Base.Nombre</td>

        <td>
            <div class="input-group">
                <div class="input-group-btn">
                    @*<a href="@Url.Action("Index","Services", routeValues: new { id = Model.Id })" class="btn btn-xs btn-success"><i class="fa fa-search"></i></a>*@

                    @*<a data-toggle="modal" onclick="editloc(@Model.Id)" href="#modal-edit" class="btn btn-primary"><i class="fa fa-pencil" data-id="@Model.Id"></i> </a>*@
                    @*<a data-toggle="modal" onclick="deleteLoc(@Model.Id)" href="#modal-delete" class="btn btn-primary"><i class="fa fa-trash-o" data-id="@Model.Id"></i> </a>*@

                    <a data-toggle="modal" onclick="Edit(@Model.Id,8)" href="#modal-edit" class="btn btn-primary"><i class="fa fa-pencil" data-id="@Model.Id"></i> </a>
                    <a data-toggle="modal" onclick="Delete(@Model.Id,8)" href="#modal-delete" class="btn btn-primary"><i class="fa fa-trash-o" data-id="@Model.Id"></i> </a>

                    @*<button type="submit" class="btn btn-xs @(Model.Active ? "btn-success" : "btn-default")">
                            <i class="fa fa-power-off" aria-hidden="true"></i>
                        </button>*@
                </div>
            </div>

            @*<a href="@Url.Action("/","Estacions", routeValues: new { id = locomotive.PK })"><i class="fa fa-search text-default"></i></a>*@
        </td>
        @*<td>
                <a href="@Url.Action("Index","Clients", routeValues: new { id = Model.Code })" class="btn btn-xs btn-success"><i class="fa fa-search"></i></a>
                <a data-toggle="modal" href="#modal-edit" class="btn btn-xs btn-primary"><i class="fa fa-pencil"></i></a>
            </td>*@
    }
</tr>

<div id="modal-edit" class="modal fade" aria-hidden="true" style="display: none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <div class="row" id="TripulantePersonaEdit">
                </div>
            </div>
        </div>
    </div>
</div>

<div id="modal-delete" class="modal fade" aria-hidden="true" style="display: none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <div class="row" id="TripulantePersonaDelete">
                </div>
            </div>
        </div>
    </div>
</div>


