@model ViewModels.Models.TripulantePersona

@using (Html.BeginForm(nameof(Trazabilidad.Controllers.TripulantePersonaController.Delete), nameof(Resources.Strings.Models.TripulantePersona), FormMethod.Post, htmlAttributes: new { @id = "modal-element-delete" }))
{
    @Html.HiddenFor(x => x.Id)

    <div class="col-sm-12">
        <h3 class="m-t-none m-b">Eliminar Tripulante</h3>
        <form role="form">
            <h4 class="m-t-none m-b">¿Desea eliminar Tripulante  ?</h4>

            <div>
                <button class="btn btn-sm btn-primary pull-right m-t-n-xs" type="submit"><strong><i class="fa fa-plus"></i> Eliminar</strong></button>
            </div>
        </form>
    </div>
}